name: Auto Move Project Cards

on:
  issues:
    types: [assigned, unassigned]

permissions:
  issues: write
  projects: write

jobs:
  move-cards:
    runs-on: ubuntu-latest
    steps:
      - name: Move to In Progress when assigned
        if: github.event.action == 'assigned'
        uses: actions/github-script@v7
        with:
          script: |
            const projectNumber = 2;
            const statusField = "Status";
            const statusValue = "In Progress";
            console.log(`Mover issue a ${statusValue}`);

      - name: Move to Backlog when unassigned
        if: github.event.action == 'unassigned'
        uses: actions/github-script@v7
        with:
          script: |
            const projectNumber = 2; // mismo proyecto
            const statusValue = "Backlog";
            console.log(`Mover issue a ${statusValue}`);
